# Engine architecture

Boundary map for the collab engine: module roster, facade pattern, DI constraint, decomposition status, boundary decisions, and forward extraction gates.

## Trigger

**Slash:** (reference only — not an invocable route)
**Prose dispatch:** (reference only — not an invocable route)
**Search phrases:** engine architecture, engine modules, registry decomposition, facade pattern, DI boundary, boundary decisions, extraction gates, module roster, seal boundary, render boundary

## Steps

1. Read this document when auditing engine module boundaries, planning extractions, or evaluating the DI constraint.
2. For `registry_state.py` public entry, state-root resolution, and project-identity binding, see [registry-state.md](registry-state.md).

## Notes

### Facade and DI boundary

`commands/collab/engine/registry.py` is the permanent executable facade. It performs package bootstrap, exposes compatibility imports for tests/importers, and delegates execution to `registry_core.py`. The compatibility core is the CLI wiring point for extracted modules; extracted modules must not import the executable facade. Leaf-to-leaf imports are allowed only when a documented dependency boundary permits them, such as the `seal_verification.py` compatibility facade that preserves existing seal imports while `registry_core.py` imports `seal_verification_logic.py` and `seal_verification_render.py` directly.

The executable facade owns no domain behavior. `registry_core.py` now owns only CLI argv dispatch (`build_parser`, `main`), the permanent seal facade pair (`invalidate_verification_seal`, `participant_verify_render`, `render_seal`), the thin `validate_registry` wrapper, the two injected write primitives (`commit_registry_and_transcript`, `commit_new_collab_artifacts`), the narrow orchestration wrappers (`record_execution`, `advance_phase`), and the parser-doc compatibility wrapper (`render_registry_cli_doc`). All command bodies, managed rendering, seal-integrity logic, and contribution validation belong to their extracted modules.

### Module roster

48 implementation modules in `commands/collab/engine/` (plus the executable facade `registry.py` and the generated-doc helper `lifecycle-doc.py`):

| Module | Owns | Does not own |
| --- | --- | --- |
| `errors.py` | shared exit helpers | any registry dependency |
| `registry_constants.py` | registry lifecycle vocabulary and policy constants | state I/O |
| `config_paths.py` | default configuration-path resolution: resolve the command config root (honoring the `COMMAND_CONFIG_ROOT` override) and derive the default roles-dir, effort-defaults, agent-model, and flag-taxonomy paths; a standalone leaf with no engine dependencies that self-resolves the repository root | the executable sys.path bootstrap, any config-file reading |
| `flag_taxonomy.py` | flag-taxonomy spec reader: parse the flag-taxonomy reference markdown (per-command headings and pipe-delimited flag rows) and project it into a class-grouped inventory (advisory / helper-enforced / generator-derived), rejecting unknown flag classes; owns the sole-use flag-row pattern and aggregates lower-tier leaves (config_paths, errors) | registry persistence, phase mutation, any write path |
| `source_contracts.py` | source-contract validation command: validate that the registry loads cleanly (no stale lock) and that required source-contract anchors are present across the flag-taxonomy, seal-verification, invariants, and planned-route reference documents under the config root; aggregates lower-tier leaves (config_paths, errors, planned_routes, registry_io) | registry persistence, phase mutation, any write path |
| `help_command.py` | help-route command: resolve a `(help <namespace> <route...>)` token list to its `commands/<namespace>/.../index.md` reference document under the repository root, reject malformed tokens and any path escaping the commands tree, and print the document; aggregates lower-tier leaves (config_paths, errors) | route dispatch, registry persistence, any write path |
| `browser.py` | browser launch: open a URI in the system browser via an injectable opener, capturing any failure (raised exception or falsey "no browser" return) as a human-readable string; a standalone leaf with no engine dependencies | URI construction, registry persistence, any write path |
| `render_commands.py` | transcript render/view/summarize command handlers: re-render the managed header and persist it (`render-status`, `render-participants`), replace the phase or latest summary (`summarize`, `re-summarize`), and emit a single phase section read-only (`transcript-view`); the cycle-blocked two-file commit is injected via `configure_render_commands` | the two-file commit implementation (core-owned, injected), header rendering, summary replacement |
| `field_commands.py` | field and participant mutation command handlers: set or force a single registry field (`set`, including reviewer assignment and the force-only `active-phase` jump), clear the reviewer (`unset reviewer`), and drop a participant from roster and turn order (`remove-participant`); validates via the importable `validate_registry_data`, and the cycle-blocked two-file commit is injected via `configure_field_commands` | the two-file commit implementation (core-owned, injected), header rendering, registry validation |
| `lifecycle_commands.py` | record lifecycle status-change command handlers: archive a record and clear it as active (`archive`), reopen a closed record (`open`), close a record after enforcing the execution/issue/seal close gates (`close`), and permanently delete a record with its transcript and contribution store (`delete`); the cycle-blocked two-file commit is injected via `configure_lifecycle_commands` | the two-file commit implementation (core-owned, injected), the close-gate predicates, header rendering, the seal verdict companion writer |
| `repair_commands.py` | integrity repair command handlers: mark a transcript repair that may have touched execution evidence (`repair-transcript`), record an out-of-scope patch outside a role's declared writeScope (`patch-out-of-scope`), and repair a completed execution's provenance — work repo, commit ids, content digests, paired signature (`repair-execution-provenance`); the cycle-blocked `invalidate_verification_seal` wrapper is injected via `configure_repair_commands` | the seal-invalidation wrapper (core-owned, injected), the git provenance helpers, digest computation |
| `reactivation_commands.py` | record reactivation command handlers: restore a record's prior content from a revision event's `_legacyBefore` snapshot (`restore-content`) and reopen a record into Action Plan or Handoff after a non-success Completion verdict (`reopen`); owns the `save_registry_with_event_type` single-file write helper; the reopen path's two cycle-blocked dependencies — the core's `invalidate_verification_seal` wrapper and the core-owned two-file commit — are injected via `configure_reactivation_commands` | the seal-invalidation wrapper and two-file commit (core-owned, injected), the revision-event primitives, registry validation |
| `onboarding_commands.py` | record onboarding command handlers: create a new moderated record with its transcript and contribution store (`init`) and join a role into an existing record's roster (`join`); owns the `ensure_init_project_metadata` init helper; the two write paths' cycle-blocked commit primitives — the core-owned `commit_new_collab_artifacts` (three-artifact create) for init and `commit_registry_and_transcript` (two-file write) for join — are injected via `configure_onboarding_commands` | the commit primitives (core-owned, injected), registry validation, transcript rendering, the init token parser |
| `speak_commands.py` | speak-path command handlers: the auto-advance lifecycle when required roles have contributed (`apply_speak_lifecycle_to_entry`/`_with_notice`), set the active record (`activate`), advance phase from a contributor list (`speak`), project a role's speak-readiness state (`speak-state`), advance from the live transcript (`speak-live`), append a rendered contribution (`speak-render`), rewrite the latest contribution (`rewrite-speak-render`), and retract (tombstone) the latest active-phase contribution (`retract-speak`); the five write paths' cycle-blocked `commit_registry_and_transcript` is injected via `configure_speak_commands`, while read/notice paths and the non-commit branch persist via the importable `save_registry` | the two-file commit (core-owned, injected), the speak-state projection, transcript rendering, contribution validation |
| `route_write_guard.py` | route write-path guard (`write-guard`): confirm a route only writes under the user-scope collab state root (the registry file, its lock, and the `records/` transcript tree), rejecting absolute paths and any out-of-root target; the `execute` route is exempt | registry state, transcript reads, commit primitives — pure path-shape validation |
| `registry_state.py` | project-identity binding, state-root resolution | registry reads/writes |
| `dispatch_forms.py` | command dispatch notation rendering | state, I/O, registry |
| `command_lines.py` | literal `registry.py` CLI invocation strings (RESUME, transcript-view) | state, I/O, registry, dispatch notation |
| `registry_core.py` | CLI argv dispatch (`build_parser`/`main`), the permanent seal facade pair, the `validate_registry` wrapper, the two injected write primitives, the narrow orchestration wrappers (`record_execution`/`advance_phase`), and the `render_registry_cli_doc` parser-doc wrapper | executable bootstrap, extracted domain ownership, any command body with extractable domain logic |
| `planned_routes.py` | route prerequisite validation, issue-bridge detection | phase mutation |
| `transcript_readers.py` | transcript phase parsing, contribution-block extraction, transcript-path resolution and per-entry reads | rendering or writes |
| `normalizers.py` | slug/title/path/scope normalization | state, I/O |
| `digests.py` | content/path digest computation and signatures | git policy |
| `handoff_shape.py` | handoff writeScope/validationCommands schema | lifecycle |
| `git_repo.py` | git subprocess reads: head, commits, content-at-ref | seal policy |
| `registry_io.py` | registry persistence, lock, resolve; validator injection | phase decisions |
| `participants.py` | participant roster, reviewer wiring, turn-order helpers | phase mutation |
| `phase_lifecycle.py` | phase sequencing, phase advancement, and lifecycle notices | registry mutation, rendering |
| `speak_state.py` | speak-eligibility state model: per-entry speak-state dict and the read-only next-command, next-line, policy-blocker, and phase-summary projections over it | registry persistence, phase mutation, rendering |
| `advisories.py` | post-action/recovery advisory rendering: post-speak/seal advisory line set and the forced active-phase recovery-advisory string | registry persistence, phase mutation, transcript reading |
| `issue_export.py` | issue-export model and command: normalizes external issue-export evidence files into validated issue dicts, detects the exported-issue handoff, and orchestrates the `export-issues` write-path command (record exported issues, conditional close, completion-summary render, persist) over two injected callbacks (`configure_issue_export`) for the cycle-blocked deps | the two-file commit implementation (core-owned, injected), the close-eligibility policy (`post_execution`-owned, injected) |
| `parser_introspection.py` | argparse parser introspection: subcommand-map extraction and per-action display-name/value-shape projections used to render the registry CLI doc | parser construction, CLI-doc orchestration |
| `content_files.py` | speak content-file readers: required-file load (rejecting missing/blank) and optional full-body load, returning trailing-newline-trimmed text | speak-command orchestration, registry persistence, rendering |
| `restore_inputs.py` | restore-input readers: validate/parse the restore event-index argument and locate a deep-copied historical collab entry by id within a pre-restore registry snapshot | restore-command orchestration, registry persistence, revision-event writing |
| `init_inputs.py` | init-command input reader: parse the raw `init` argv token stream into a validated `(title, agent id, reviewer, open-requested, participant-verification, terminal, work-repo)` tuple, rejecting duplicate/unknown flags and missing flag values; owns the sole-use role-key pattern and aggregates lower-tier leaves (errors, normalizers, registry_constants) | init-command orchestration, registry persistence, phase mutation, any write path |
| `query_commands.py` | read-only CLI query/projection command handlers: load and project a single read view (registry path, role roster rows, reviewer/handoff state, timestamps, summary role), print it, and return an exit code; aggregates lower-tier domain leaves (roles, participants, handoff_shape, registry_io, normalizers, transcript_readers) | registry persistence, phase mutation, any write path |
| `inspection_commands.py` | read-only inspection/report command handlers: list collabs, project the revision-event log, render a single-entry status view, compute drift, and audit closed collabs — load and project a multi-entry or transcript-derived read view, print it, and return an exit code; aggregates lower-tier domain leaves (registry_io, registry_state, registry_constants, normalizers, transcript_readers, participants, seal_verification, contribution_store, diff, execution, effort) | registry persistence, phase mutation, any write path |
| `post_execution.py` | post-execution lifecycle projections: given an entry and its assigned roles, compute close-eligibility after execution and the next-line guidance string after execution — read-only projections over high-level verification/issue-export/seal state, injected into `execution.py` as callbacks because that lower-tier module cannot import the seal_verification/issue_export leaves directly; aggregates lower-tier leaves (dispatch_forms, execution, issue_export, participants, seal_verification, speak_state) | registry persistence, phase mutation, any write path |
| `execution.py` | execution checks, run-plan support, write-scope enforcement, execution state recording | seal |
| `contribution_store.py` | contribution-store path and shape helpers | registry state, rendering, write path |
| `contribution_validation.py` | speak-time contribution gates, moderator contribution normalization | rendering, write path |
| `diff.py` | read-only collab drift comparison | registry writes, rendering, state mutation |
| `registry_validation.py` | schema validation | advisory math, write path |
| `effort.py` | advisory math | schema validation, write path |
| `transcript_render.py` | managed rendering: header, TOC, all `<details>` blocks, contribution blocks, effort-override banners | registry state, phase lifecycle, write-path dispatch, CLI entry-point logic |
| `seal_verification.py` | compatibility exports for the split verification boundary; existing engine leaves import this facade while the core imports the split modules directly | domain ownership, registry persistence, phase lifecycle, CLI dispatch |
| `seal_verification_logic.py` | seal state readers, completion and participant-verification state, stale-seal invalidation, content-integrity and git-state gates, verdict construction and validation, cap-exit dispatch, chartered-deliverables coverage, and verification restart | participant-verify rendering, assessment/seal rendering, verdict/seal write entry points, registry persistence, CLI dispatch |
| `seal_verification_render.py` | participant-verify rendering, assessment/seal rendering, reviewer findings, summary/history rendering, and the four write/dispatch entry functions (`participant_verify_render`, `seal_write`, `record_verdict`, `render_seal`) that call the logic module explicitly | stale-seal trigger decisions, participant-verification state ownership, verdict validation, content-integrity policy, phase lifecycle, participant roster management |

### Decomposition status

The executable facade target and the registry-core domain extraction target are complete:

1. Pure readers and parsers — done (#56: normalizers, digests, handoff\_shape, git\_repo, registry\_io)
2. Route-planning helpers — done (#56: participants, phase\_lifecycle, execution, dispatch\_forms)
3. Managed rendering engine — done (#57: transcript\_render.py)
4. Seal/verification engine — done (#58: seal\_verification.py)
5. Speak-time contribution validation — done (contribution\_validation.py, contribution\_store.py)
6. Registry validation, effort math, and drift comparison — done (registry\_validation.py, effort.py, diff.py)
7. Speak-eligibility state model — done (speak\_state.py)
8. Post-action/recovery advisory rendering — done (advisories.py)
9. Issue-export read/validate model — done (issue\_export.py); the `export_issues` command orchestrator was subsequently drained too (see wave 23), so the leaf now owns the full read→validate→write-path command
10. Argparse parser introspection — done (parser\_introspection.py); `render_registry_cli_doc` remains a compatibility wrapper in the core (it composes the core-owned `build_parser`)
11. Speak content-file readers — done (content\_files.py); the `render_speak`/`render_re_speak` write-path commands that consume them remain compatibility wrappers in the core
12. Restore-input readers — done (restore\_inputs.py); the `restore_collab_content` write-path command that consumes them remains a compatibility wrapper in the core
13. Read-only CLI query/projection command handlers — done (query\_commands.py); a cohesive read-only command leaf aggregating lower-tier domain leaves, leaving the core's `*_command` surface limited to write-path and validation handlers
14. Read-only inspection/report command handlers — done (inspection\_commands.py); the heavier read views (`list_collabs`, `log_command`, `status_view`/`print_status_view`, `diff_command`, `audit_closed`) that read transcripts, scan all collabs, compute drift, and audit closed collabs, moved into a sibling read-only command leaf; the core's surviving `*_command`/render wrappers are now write-path or validation handlers
15. Init-command input reader — done (init\_inputs.py); the init argv token parser (`parse_init_tokens`) and its sole-use role-key pattern (`ROLE_KEY_RE`) moved into a read-only input-reader leaf paralleling `restore_inputs.py`, leaving `init_collab` in the core to orchestrate the write path over the parsed tuple
16. Post-execution lifecycle projections — done (post\_execution.py); the post-execution close-eligibility decision (`close_eligible_after_execution`) and next-line guidance projection (`next_line_after_execution`) moved into a read-only top-tier aggregator leaf, which the core re-imports and passes as the `ExecutionCallbacks` DI callbacks; the leaf reaches the high-level seal_verification/issue_export leaves that `execution.py` itself cannot import, preserving the callback-injection boundary
17. Default configuration-path resolution — done (config\_paths.py); `resolve_config_root` and the `DEFAULT_CONFIG_ROOT`/`DEFAULT_ROLES_DIR`/`DEFAULT_EFFORT_PATH`/`DEFAULT_AGENT_MODEL_PATH`/`DEFAULT_FLAG_TAXONOMY_PATH` constants moved into a standalone leaf that self-resolves the repository root (the same idiom `advisories.py` already uses), which the core re-imports; the core retains its own bootstrap `ROOT` because sys.path setup must run before any sibling-leaf import resolves, so the config-path layer is now owned independently and the validation/`flag_inventory` handlers that depend on these constants are unblocked for a future drain
18. Flag-taxonomy spec reader — done (flag\_taxonomy.py); the `flag_inventory` read-only command and its sole-use flag-row pattern (`FLAG_ROW_RE`) moved into a focused leaf that reads the flag-taxonomy reference spec and projects the class-grouped inventory, depending only on `config_paths` and `errors`; first drain unblocked by the config-path extraction (it could not move while `DEFAULT_FLAG_TAXONOMY_PATH` lived in the core without a leaf→core cycle)
19. Source-contract validation command — done (source\_contracts.py); the `validate` command (`validate_command`) and its source-contract anchor checks (`validate_source_contracts`, `require_source_text`) moved into a focused leaf that asserts the registry loads cleanly and the required anchors are present across the seal-verification, invariants, and planned-route reference docs; second drain unblocked by the config-path extraction (the checks depend on `DEFAULT_CONFIG_ROOT`/`DEFAULT_FLAG_TAXONOMY_PATH`, which previously kept them pinned to the core)
20. Help-route command — done (help\_command.py); the `route_help_command` handler moved into a focused leaf that resolves and prints a route's `index.md` reference doc under the repository root; it imports the raw repo-root `ROOT` from `config_paths` (identical to the core's bootstrap `ROOT`) rather than duplicating the root-resolution idiom, so no second source of truth is introduced
21. Reviewer-rewrite notice projection — done (folded into transcript\_render.py, no new module); `reviewer_notice_for_rewrite` moved from the core into the managed-rendering leaf that already defines `latest_contribution_timestamp` and imports `participants` for `active_reviewer_role`, a cohesive home that drains the core without adding a singleton module; the core re-imports it for its `render_re_speak` caller
22. Browser-launch utility — done (browser.py); the `open_browser_uri` opener-injectable browser-launch helper moved into a standalone stdlib-only leaf, isolating the browser side-effect (and its failure-to-string capture) from the core; the core re-imports it for its `init_collab` write-path caller, which retains `webbrowser.open_new_tab` as the opener default it threads through
23. Export-issues command (first sanctioned write-path drain) — done (issue\_export.py); the `export_issues` orchestrator moved out of the core into the issue-export leaf that already owned its read/validate model, completing #9. This is the first extraction to relocate a *write-path* command rather than a read-only helper, so it does not satisfy the blanket [V-shape] write-path-freeze; instead it follows the established DI-seam discipline (`configure_registry_facade`/`configure_registry_io`): the two dependencies the leaf cannot import without a cycle — the core-owned `commit_registry_and_transcript` two-file commit and the `post_execution`-owned `close_eligible_after_execution` decision (the leaf is already imported *by* `post_execution`, so a direct import would form a cycle) — are injected via a new `configure_issue_export` seam the core invokes alongside its sibling facade wiring. The relocation is verbatim; verification rests on the pre-existing export-issues suite (happy-path `export-issues-flow` plus the role/phase/terminal/closed/pending-execution/registry-target guard tests), all green after the move
24. Transcript render/view/summarize commands (second write-path drain) — done (render\_commands.py); five command handlers moved out of the core into a new cohesive leaf: `render_status` and `render_participants` (re-render the managed header and persist), `summarize_collab` and `re_summarize_collab` (replace the phase or latest summary), and the read-only `transcript_view` (emit one phase section). All five had no external callers and shared a single core-defined dependency — `commit_registry_and_transcript` — injected via a new `configure_render_commands` seam wired alongside `configure_issue_export`; every other dependency already lived in a leaf (`registry_io`, `transcript_readers`, `transcript_render`, `seal_verification`, `inspection_commands`, `registry_constants`), and because nothing imports the new module no cycle is possible. Following the write-path-drain precedent from #23, the four write-path commands route their commit through the injected callable (guarded by `_require_commit`); `transcript_view` is pure-read and uses no seam. Relocation is verbatim; the core re-imports all five for dispatch. Full suite green (193 scripts)
25. Field and participant mutation commands (third write-path drain) — done (field\_commands.py); four handlers moved out of the core into a new cohesive leaf: `set_field` (set or force a single field, including reviewer assignment and the force-only `active-phase` jump), `unset_field` (clear the reviewer), `remove_participant` (drop a role from roster and turn order), and the shared `clear_reviewer` helper they both call. None had external callers (only core dispatch and the in-cluster `clear_reviewer`). The cluster had two core-coupled needs: the core-defined `commit_registry_and_transcript`, injected via a new `configure_field_commands` seam wired alongside `configure_render_commands`; and the core's thin `validate_registry` wrapper, which the leaf sidesteps by calling the importable `validate_registry_data` (its underlying `registry_validation.validate_registry`) bound with `DEFAULT_ROLES_DIR` directly — so only one injection is required. Every other dependency already lived in a leaf (`registry_io`, `participants`, `registry_constants`, `execution`, `git_repo`, `seal_verification`, `advisories`, `transcript_readers`, `transcript_render`, `roles`); because nothing imports the new module no cycle is possible. The three write-path commands route their commit through the injected callable (guarded by `_require_commit`). Relocation is verbatim apart from the `validate_registry` → `validate_registry_data(..., DEFAULT_ROLES_DIR)` inline and the commit-callable indirection; the core re-imports all four for dispatch. Full suite green (193 scripts)
26. Record lifecycle status-change commands (fourth write-path drain) — done (lifecycle\_commands.py); four handlers moved out of the core into a new cohesive leaf: `archive_collab` (archive and clear active), `open_collab` (reopen a closed record), `close_collab` (close after enforcing the execution-unchecked / issue-terminal / reviewer-backed-seal close gates), and `delete_collab` (permanently remove the record, transcript, and contribution store). None had external callers (only core dispatch). The cluster shares a single core-defined dependency — `commit_registry_and_transcript` — injected via a new `configure_lifecycle_commands` seam wired alongside `configure_field_commands`; every other dependency already lived in a leaf, including the heavier close-gate set (`execution.completed_execution_unchecked_items`/`issue_terminal`/`seal_terminal`/`all_execution_completed`, `participants.reviewer_backed`, `issue_export.exported_issue_handoff_present`, `seal_verification.invalidate_seal_on_content_drift`/`successful_verdict`/`write_seal_verdict_companion`, `contribution_store.contribution_store_path_for_entry`, `phase_lifecycle`, `advisories`, `speak_state`), and because nothing imports the new module no cycle is possible. The three commit-bearing commands route their write through the injected callable (guarded by `_require_commit`); `delete_collab` persists via the importable `save_registry` and needs no seam. Relocation is verbatim; the core re-imports all four for dispatch. Full suite green (193 scripts)
27. Integrity repair commands (fifth write-path drain, first non-commit seam) — done (repair\_commands.py); three handlers moved out of the core into a new cohesive leaf: `transcript_repair` (flag a repair that touched execution evidence), `out_of_scope_patch` (record a patch outside a role's declared writeScope), and `repair_execution_provenance` (repair a completed execution's work repo / commit ids / content digests / paired signature). None had external callers (only core dispatch). Unlike the prior four drains, the shared core-coupled dependency is **not** `commit_registry_and_transcript` — these commands persist via the importable `save_registry` — but the core's `invalidate_verification_seal` wrapper, which cannot be imported into a leaf because it threads `participants`-derived predicates (`reviewer_backed`, `participant_verification_incomplete`/`_enabled`) into `seal_verification`'s module globals before delegating, and is therefore injected via a new `configure_repair_commands` seam wired alongside `configure_lifecycle_commands`. Every other dependency already lived in a leaf (`registry_io`, `participants`, `git_repo`, `normalizers`, `handoff_shape`, `digests`, `post_execution`, `seal_verification`); because nothing imports the new module no cycle is possible. The three commands route seal invalidation through the injected callable (guarded by `_require_invalidate`). `record_execution` stays core-resident: it is already a narrow orchestration wrapper that assembles `ExecutionCallbacks` and delegates to the `record_execution_state` leaf, matching the target end-state. Relocation is verbatim; the core re-imports all three for dispatch. Full suite green (193 scripts)
28. Record reactivation commands (sixth write-path drain, first two-seam leaf) — done (reactivation\_commands.py); two handlers plus a shared write helper moved out of the core into a new cohesive leaf: `restore_collab_content` (restore a record's prior content from a revision event's `_legacyBefore` snapshot), `reopen_collab` (reopen into Action Plan or Handoff after a non-success Completion verdict), and the `save_registry_with_event_type` single-file write helper that `restore_collab_content` is the sole caller of (so it moved with it rather than staying core-resident). None had external callers (only core dispatch and the in-cluster call). This is the first leaf needing **two** injected seams: `reopen_collab` couples to both the core's `invalidate_verification_seal` wrapper (non-inlinable, as in repair) and the core-owned `commit_registry_and_transcript` two-file write, both injected via a new `configure_reactivation_commands` seam wired alongside `configure_repair_commands`. `restore_collab_content` and `save_registry_with_event_type` need no seam: they sidestep the core's thin `validate_registry` wrapper by calling the importable `validate_registry_data(..., DEFAULT_ROLES_DIR)` directly (as field/lifecycle do) and persist via the importable revision-event primitives. Every other dependency already lived in a leaf (`registry_io`, `registry_state`, `restore_inputs`, `participants`, `contribution_validation`, `digests`, `dispatch_forms`, `seal_verification`, `advisories`, `speak_state`, `transcript_readers`, `transcript_render`); because nothing imports the new module no cycle is possible. `reopen_collab` routes its two core couplings through the injected callables (guarded by `_require_invalidate` / `_require_commit`). Relocation is verbatim apart from the `validate_registry` → `validate_registry_data(..., DEFAULT_ROLES_DIR)` inline and the two callable indirections; the core re-imports all three names for dispatch. Full suite green (193 scripts)
29. Record onboarding commands (seventh write-path drain, second two-seam leaf) — done (onboarding\_commands.py); two handlers plus the init-only `ensure_init_project_metadata` helper moved out of the core into a new cohesive leaf: `init_collab` (create a new moderated record, transcript, and contribution store) and `join_participants` (join a role into an existing roster). Neither had external callers (only core dispatch); `ensure_init_project_metadata` is called only by `init_collab` so it moved with it. Like reactivation this leaf needs **two** injected seams, but a different pair: `init_collab` couples to the core-owned `commit_new_collab_artifacts` (three-artifact create) and `join_participants` to `commit_registry_and_transcript` (two-file write), both injected via a new `configure_onboarding_commands` seam — placed after the `commit_new_collab_artifacts` definition (which sits below the other configure calls), not alongside them. `join_participants` sidesteps the core's thin `validate_registry` wrapper via the importable `validate_registry_data(..., DEFAULT_ROLES_DIR)` (as field/lifecycle/reactivation do). Every other dependency already lived in a leaf (`init_inputs`, `registry_io`, `registry_state`, `registry_constants`, `normalizers`, `participants`, `git_repo`, `contribution_store`, `command_lines`, `browser`, `dispatch_forms`, `transcript_readers`, `transcript_render`, `advisories`, and the top-level `roles`); because nothing imports the new module no cycle is possible. This drain also moved the `'terminal': terminal` entry-construction literal that `planned_routes.workflow_model_selection_gaps` greps for as a structural guard, so its `helper_text` source list was extended to include `onboarding_commands.py` alongside `registry.py`/`registry_core.py` — the guard follows the field-persistence logic to its new home. Relocation is verbatim apart from the `validate_registry` inline and the two commit-callable indirections; the core re-imports all three names for dispatch. Full suite green (193 scripts)
30. Speak-path commands (eighth write-path drain, largest single drain) — done (speak\_commands.py); nine handlers moved out of the core into a new cohesive leaf: the pure auto-advance lifecycle `apply_speak_lifecycle_to_entry`/`apply_speak_lifecycle_with_notice`, the `die_with_resume` helper, `activate_collab` (`activate`), `speak_lifecycle` (`speak`), `speak_state` (`speak-state`), `speak_lifecycle_live` (`speak-live`), `render_speak` (`speak-render`), and `render_re_speak` (`rewrite-speak-render`). The only core-defined dependency across the whole family is `commit_registry_and_transcript`, injected via a new `configure_speak_commands` seam (placed alongside `configure_onboarding_commands`, after the commit primitive is defined); the four write paths route through the injected callable (guarded by `_require_commit`), while the read/notice helpers and `activate`/`speak`'s no-transcript branch persist via the importable `save_registry`. A definition-only dependency analysis confirmed every other name (≈70 across `participants`, `contribution_validation`, `transcript_render`, `transcript_readers`, `speak_state`, `phase_lifecycle`, `registry_io`, `registry_constants`, `content_files`, `contribution_store`, `handoff_shape`, `effort`, `seal_verification`, `advisories`, `command_lines`, `normalizers`, `dispatch_forms`) already lived in a leaf and resolved to its canonical owner (one homonym, `print_post_action_advisories`, imported from `advisories` per the existing precedent); because nothing imports the new module no cycle is possible. Two source-path-coupled structural guards followed the relocation: `tests/.../speak-guards.test.sh` (which regex-extracts `render_re_speak`'s body to assert it stays role-scoped) was repointed from `registry_core.py` to `speak_commands.py`, and `engine/audit-reviewer-conclusion.sh` plus its test (which assert `render_speak` calls `validate_reviewer_conclusion_gates` before mutation) were repointed the same way and the test fixture extended to copy `speak_commands.py`. `advance_phase` stays core-resident: like `record_execution` it is already a narrow `PhaseLifecycleCallbacks` orchestration wrapper delegating to `advance_phase_state`, matching the target end-state. Relocation is verbatim apart from the commit-callable indirection; the core re-imports all nine names for dispatch. Full suite green (193 scripts)
31. Retract-speak command (folded into the speak family) — done (speak\_commands.py); `retract_latest_contribution` (`retract-speak`, which tombstones the latest active-phase contribution and retains it for audit history) moved out of the core and into the existing `speak_commands.py` rather than a new module — it is a contribution-mutation sibling of `render_re_speak`/`render_speak` and shares their exact seam, `commit_registry_and_transcript` (already injected via `configure_speak_commands`), so no new module and no new seam were needed. Only two leaf imports were added to the module (`contribution_store.mark_contribution_store_record_retracted`, `transcript_render.rendered_retracted_content_block`); every other dependency was already imported for the speak family. Relocation is verbatim apart from the commit-callable indirection; the core re-imports the name for dispatch. This leaves `render_registry_cli_doc` (parser-introspection, coupled to the core-resident `build_parser`) and `write_guard` (a standalone path-guard with no core coupling) as the only remaining non-keeper command bodies in the core. Full suite green (193 scripts)
32. Route write-path guard (final command-body drain; end-state reached) — done (route\_write\_guard.py); `write_guard` (`write-guard`, which confirms a route only writes under the user-scope collab state root) moved out of the core into a new focused leaf. It is the simplest possible extraction: the body couples to nothing but `errors.die` and `pathlib.Path` — no registry state, no transcript reads, no commit primitive, hence no seam — and because nothing imports the new module no cycle is possible. A lone-function module is justified here against the usual 2+-cohesive-functions preference because it is the last non-keeper command body and removing it satisfies the directive end-state exactly. With this drain `registry_core.py` holds only the target set: argv parsing (`build_parser`), dispatch (`main`), the permanent seal facade pair (`invalidate_verification_seal`, `participant_verify_render`, `render_seal`), the thin `validate_registry` wrapper, the two injected write primitives (`commit_registry_and_transcript`, `commit_new_collab_artifacts`), the narrow orchestration wrappers (`record_execution`, `advance_phase`), and the parser-doc compatibility wrapper (`render_registry_cli_doc`, kept per wave 10). Relocation is verbatim; the core re-imports the name for dispatch. Full suite green (193 scripts)
33. Seal/verification boundary split — done (`seal_verification_logic.py`, `seal_verification_render.py`); the former oversized verification module split along the one-way render/logic seam. State readers, participant-verification state, stale-seal triggers, content-integrity gates, verdict construction, cap-exit policy, chartered-deliverables coverage, and verification restart moved to the logic leaf. Participant-verify rendering, assessment/seal rendering, summary/history rendering, reviewer findings, and the write/dispatch entry functions moved to the render leaf, which calls the logic leaf explicitly. `seal_verification.py` remains as a compatibility facade for existing leaf imports, while `registry_core.py` imports both split modules directly for its permanent seal facade and CLI dispatch. The guardrail packet: source cluster `seal_verification.py`; destination modules `seal_verification_logic.py` and `seal_verification_render.py`; public compatibility imports retained through `seal_verification.py`; render assertions covered by the transcript render byte-identical fixture and participant-verify flow; write-path freeze confirmed by preserving `seal_write`, `record_verdict`, `participant_verify_render`, and `render_seal` as render-leaf entry points with the existing registry-core dispatch. Direct-import isolation tests now cover `onboarding_commands.py`, `reactivation_commands.py`, and `speak_commands.py` handlers without going through the CLI. Full suite green (193 scripts)

`registry.py` is now thin: package bootstrap, compatibility exports, and executable delegation only. The decomposition of `registry_core.py` into focused, independently-testable leaf modules (`phase_lifecycle.py`, `contribution_validation.py`, `contribution_store.py`, `effort.py`, `transcript_render.py`, `seal_verification_logic.py`, `seal_verification_render.py`, `speak_state.py`, `advisories.py`, `issue_export.py`, `parser_introspection.py`, `content_files.py`, `restore_inputs.py`, `query_commands.py`, `inspection_commands.py`, `init_inputs.py`, `post_execution.py`, `config_paths.py`, `flag_taxonomy.py`, `source_contracts.py`, `help_command.py`, `browser.py`, `render_commands.py`, `field_commands.py`, `lifecycle_commands.py`, `repair_commands.py`, `reactivation_commands.py`, `onboarding_commands.py`, `speak_commands.py`, `route_write_guard.py`) has reached its target end-state: the compatibility core is now limited to argv parsing (`build_parser`, `main`), the permanent seal facade pair, the thin `validate_registry` wrapper, the two injected write primitives, the narrow orchestration wrappers (`record_execution`, `advance_phase`), and the parser-doc compatibility wrapper (`render_registry_cli_doc`). No command body with extractable domain logic remains in the core. Further work is maintenance of this boundary — any new command handler lands in (or as) an owning leaf, never in the core — rather than continued draining.

### Boundary decisions

**Seal verification (split boundary):** `seal_verification_logic.py` owns verification state, stale-seal triggers, content-integrity gates, verdict construction, cap-exit policy, and chartered-deliverables coverage. `seal_verification_render.py` owns participant-verify/assessment/seal rendering, summary/history rendering, reviewer-findings blocks, and the write/dispatch entry functions (`participant_verify_render`, `seal_write`, `record_verdict`, `render_seal`) that call the logic module explicitly. `seal_verification.py` is a compatibility facade only; existing leaves may import it for stable helper names, while `registry_core.py` imports the split modules directly.

**`transcript_render.py` (kept whole — managed-rendering boundary):** Header scaffolding, TOC management, and all `<details>` block construction share the `rendered_collapsible_block` primitive and the single-owner invariant: no caller constructs a `<details>` block outside this module. Splitting prematurely would compromise that invariant. If the module later warrants division, the documented split boundary is: `header_render.py` (header, TOC, `insert_toc_entry`) and `contribution_render.py` (contribution/collapsible-block rendering, excerpt/full-body handling, effort-override banners).

### Forward extraction gates

Every future extraction item must satisfy all three gates before merging:

**[P1-render]** Byte-identical render gate: any item touching managed rendering (participants table, TOC, header, `<details>` scaffolding) must run the rendering helper before and after against a fixed fixture transcript and assert a zero-byte diff. Prose "behavior-equivalent" review does not satisfy this gate. Rationale: one whitespace byte of render drift silently breaks every route asserting managed-section bytes (Invariant #1).

**[P2-seal]** Paired staleness-test gate: each stale-seal trigger relocated during a seal/verification extraction must ship a shell test asserting the trigger still invalidates the seal after the move. Rationale: a seal that "appears valid but covers different evidence" is a silent failure; this gate makes it loud.

**[V-shape]** Per-item guardrail packet (must appear in every extraction collab's Action Plan): source cluster, destination module, public imports retained by `registry.py`, byte-identical render assertions where [P1-render] applies, and write-path freeze confirmation.

Commit discipline: each extraction item lands as its own atomically-scoped, accurately-titled commit. Bundling a design move with behavioral assertions in one commit mislabels the record; git history is the canonical record of past outcomes.

### Directive of record

"Keep splitting the oversized registry core into focused, independently testable parts. One enormous module is hard to reason about and risky to change; cohesive units shrink the blast radius of every edit."  
— Collab 2026-06-04-registry-decomposition

*Promoted from the `registry.py` header, which now carries a pointer to this file.*
