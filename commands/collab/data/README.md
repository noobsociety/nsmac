# Collab namespace data

Namespace-local data files support the `commands/collab/` slice. JSON files cannot carry inline annotations, so this README describes the directory contents.

- `collab.json` — advisory data for collab routes. Governs per-route caller recommendations consumed by `platform/tooling/command-advisories.py`.
