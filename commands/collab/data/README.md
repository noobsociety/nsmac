Namespace-local data files for the `commands/collab/` slice. JSON files cannot carry inline annotations — this README is the descriptor for the directory's contents.

- `collab.json` — advisory data for collab routes. Governs per-route caller recommendations consumed by `platform/tooling/command-advisories.py`.
