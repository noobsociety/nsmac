# Command Reference

This spec section is human-authored and lives outside the generator markers below. Everything between `<!-- BEGIN GENERATED:COMMAND_REFERENCE -->` and `<!-- END GENERATED:COMMAND_REFERENCE -->` is generator-owned and must not be hand-edited. Regenerate by running `platform/tooling/command-reference.py --render`.

<!-- BEGIN GENERATED:COMMAND_REFERENCE -->
_Generated by `platform/tooling/command-reference.py`; do not edit this block by hand._

## agent
### install
`(agent install [--force])`
> **Recommended:** execution capability; high effort - Writes the multi-agent scaffold into a fresh repo; wrong-tier execution risks silent template drift.
  `--force`    optional    literal: `present`
### patch
`(agent patch [--force])`
> **Recommended:** execution capability; high effort - Wrong-tier patches risk silent template drift.
  `--force`    optional    literal: `present`
### upgrade
`(agent upgrade [--force])`
> **Recommended:** execution capability; high effort - Overwrites installed scaffold files against the current templates; misjudged diffs corrupt the consumer repo.
  `--force`    optional    literal: `present`

## collab
### activate
`(collab activate <record>)`
> **Recommended:** execution capability; low effort - Switches the active collab pointer in the registry.
  `<record>`    required    dynamic: `collab slug, id, or #N from the registry`
### advance
`(collab advance)`
> **Recommended:** execution capability; low effort - Phase transition gated by the registry; helper enforces preconditions.
### archive
`(collab archive [<target>])`
> **Recommended:** execution capability; low effort - Soft-deletes the collab from the active set.
  `<target>`    optional    dynamic: `collab slug, id, or #N; defaults to active collab`
### close
`(collab close [--no-summary])`
> **Recommended:** execution capability; low effort - Closes a completed collab and emits the close summary.
  `--no-summary`    optional    literal: `present`
### delete
`(collab delete [<target>])`
> **Recommended:** execution capability; low effort - Permanently removes the collab record and registry entry.
  `<target>`    optional    dynamic: `collab slug, id, or #N; defaults to active collab`
### diff
`(collab diff [<target>])`
> **Recommended:** transformation capability; low effort - Compares registry-derived content state against the content-only transcript sections.
  `<target>`    optional    dynamic: `collab slug, id, or #N; defaults to active collab`
### export-issues
`(collab export-issues <evidence-file>)`
> **Recommended:** execution capability; high effort - Records issue-terminal handoff evidence and may close the collab without reviewer seal state.
  `<evidence-file>`    required    type: `JSON issue export evidence path`
### init
`(collab init "<name>" [--reviewer <role>] [--terminal <seal|issue>] [--no-participant-verification] [--work-repo <path>] [--preview])`
> **Recommended:** composition capability; high effort - Composes a new multi-section transcript and registry entry; downstream phases depend on the initial shape.
  `<name>`    required    type: `title text`
  `--reviewer`    optional    dynamic: `role keys from commands/collab/reference/roles/`
  `--terminal`    optional    literal: `seal | issue`
  `--no-participant-verification`    optional    type: `boolean flag`
  `--work-repo`    optional    type: `absolute path to project git work tree`
  `--preview`    optional    type: `boolean flag`
### join
`(collab join --role <role>)`
> **Recommended:** execution capability; low effort - Registry write that captures at-join identity; bounded helper call.
  `--role`    required    dynamic: `role keys from commands/collab/reference/roles/`
### list
`(collab list [--status <open|closed|archived>])`
> **Recommended:** transformation capability; low effort - Renders registry state as a list.
  `--status`    optional    literal: `open | closed | archived`
### log
`(collab log [<target>])`
> **Recommended:** transformation capability; low effort - Reads the append-only revision store and emits the registry changelog.
  `<target>`    optional    dynamic: `collab slug, id, or #N; defaults to active collab`
### open
`(collab open)`
> **Recommended:** execution capability; low effort - Opens a new collab from existing record state.
### participant verify
`(collab participant verify [<role>])`
> **Recommended:** reasoning capability; xhigh effort - Per-participant verification judgment; convergent gate before reviewer seal.
  `<role>`    optional    dynamic: `registered participant role key in the active collab`
### remove participant
`(collab remove participant <role>)`
> **Recommended:** execution capability; low effort - Removes a participant from the registry roster.
  `<role>`    required    dynamic: `role keys from commands/collab/reference/roles/`
### reopen
`(collab reopen <action-plan | handoff> [<target>])`
> **Recommended:** reasoning capability; high effort - Reopen rationale must be preserved and justified; the lifecycle decision affects downstream cap-exit actions.
  `<action-plan | handoff>`    required    literal: `action-plan | handoff`
  `<target>`    optional    dynamic: `collab slug, id, or #N; defaults to active collab`
### restore
`(collab restore)`
> **Recommended:** execution capability; low effort - Restores an archived collab from registry state.
### retract speak
`(collab retract speak [--reason <text>])`
> **Recommended:** execution capability; low effort - Tombstones the current role's latest active-phase contribution; bounded transcript edit.
  `--reason`    optional    type: `free text`
### rewrite execution
`(collab rewrite execution)`
> **Recommended:** execution capability; medium effort - Rewrites the last execution record in place; preserves historical metadata.
### rewrite speak
`(collab rewrite speak)`
> **Recommended:** composition capability; medium effort - Replaces the visible contribution while preserving audit history.
### rewrite summary
`(collab rewrite summary)`
> **Recommended:** composition capability; medium effort - Replaces the existing summary while preserving its outline.
### run plan
`(collab run plan)`
> **Recommended:** execution capability; high effort - Implements role-assigned action-plan items in source; scoped validation gates the execution record.
### seal verification
`(collab seal verification [--cap-exit <action>] [--outcome <outcome>] [--restore-target <target>] [--restore-reason <reason>] [--evidence <json>] [--failure-category <category>])`
> **Recommended:** reasoning capability; xhigh effort - Reviewer seal pass; one bad judgment stales the seal and blocks close.
  `--cap-exit`    optional    literal: `reopen-action-plan | reopen-handoff | follow-up-collab | archive`
  `--outcome`    optional    literal: `success | incomplete | failed`
  `--restore-target`    optional    dynamic: `registered phase name; one of action-plan or handoff`
  `--restore-reason`    optional    dynamic: `causal justification; required when outcome is incomplete or failed`
  `--evidence`    optional    dynamic: `JSON object of read-only anchors (transcript ids, revision, committed paths, entry ids)`
  `--failure-category`    optional    dynamic: `causal label; optional`
### set
`(collab set <field> <value>)`
> **Recommended:** execution capability; low effort - Scoped metadata edit on the resolved collab.
  `<field>`    required    literal: `title | description | turn-order | reviewer | reviewer-optional-phases | active-phase | work-repo`
  `<value>`    required    type: `field-specific replacement value`
### show flags
`(collab show flags)`
> **Recommended:** transformation capability; low effort - Renders the generated flag inventory.
### show policy
`(collab show policy)`
> **Recommended:** transformation capability; low effort - Renders the resolved gate policy for the active collab.
### show verdict
`(collab show verdict [<target>])`
> **Recommended:** transformation capability; low effort - Renders verdict metadata from registry state.
  `<target>`    optional    dynamic: `collab slug, id, or #N; defaults to active collab`
### speak
`(collab speak [<message>] [--turn-order <key>...] [--verbatim])`
> **Recommended:** reasoning capability; high effort - Contribution must integrate with prior speakers and the active-phase contract.
> **Recommended for `pa`:** reasoning capability; xhigh effort - Convergent gate reviewer pass; one bad judgment propagates across the verdict.
  `<message>`    optional    type: `free text`
  `--turn-order`    optional    dynamic: `registered participant role keys in the active collab`
  `--verbatim`    optional    type: `boolean flag`
### status
`(collab status [<target>])`
> **Recommended:** transformation capability; low effort - Reads registry state and displays the current workflow state of the resolved collab.
  `<target>`    optional    dynamic: `collab slug, id, or #N; defaults to active collab`
### summarize
`(collab summarize [<target>])`
> **Recommended:** reasoning capability; medium effort - Generates a concise agent-authored summary of the active phase's contributions.
  `<target>`    optional    dynamic: `collab slug, id, or #N; defaults to active collab`
### unset
`(collab unset reviewer)`
> **Recommended:** execution capability; low effort - Clears scoped metadata on the resolved collab.
### write summary
`(collab write summary)`
> **Recommended:** composition capability; high effort - Synthesizes the completion narrative from transcript state.

## doc
### write changelog
`(doc write changelog <atomic | squash>)`
  `<atomic | squash>`    required    literal: `atomic | squash`
### write manual
`(doc write manual)`
### write readme
`(doc write readme)`

## git
### commit
`(git commit <atomic | squash>)`
  `<atomic | squash>`    required    literal: `atomic | squash`
### issue
`(git issue <create | implement> <goal>)`
  `<create | implement>`    required    literal: `create | implement`
  `<goal>`    required    type: `free text issue goal`

## quality
### assess game
`(quality assess game <project>)`
> **Recommended:** reasoning capability; xhigh effort - Game-engineering verdict over loop, canvas, and asset surfaces; restore cost is high.
  `<project>`    required    type: `game project root path`
### assess interface
`(quality assess interface <image> <project>)`
> **Recommended:** reasoning capability; xhigh effort - Verdict artifact; false-clean propagates as confidence.
  `<image>`    required    type: `image path or attachment`
  `<project>`    required    type: `project root path`
### assess operations
`(quality assess operations <project>)`
> **Recommended:** reasoning capability; xhigh effort - Build and operations verdict over CI, deploy, and tooling mechanics; misread regressions propagate to release.
  `<project>`    required    type: `project root path`
### assess web
`(quality assess web <project>)`
> **Recommended:** reasoning capability; xhigh effort - Multi-artifact web-stack verdict; cross-cutting concerns require sustained judgment.
  `<project>`    required    type: `web project root path`
### show notes
`(quality show notes)`
> **Recommended:** transformation capability; low effort - Renders stored notes.
### tune
`(quality tune <interface | web | game | operations>)`
> **Recommended:** composition capability; high effort - Specialist pass plus cross-cutting Criteria audit; tunes prior assessment into a coherent revision.
  `<interface | web | game | operations>`    required    literal: `interface | web | game | operations`
<!-- END GENERATED:COMMAND_REFERENCE -->
